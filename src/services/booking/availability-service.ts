// import { BookingRepository, TimeRange } from '../../models/booking/repositories/booking-repository';

// export async function isResourceAvailable(
//   resourceId: string,
//   timeRange: TimeRange,
//   bookingRepo: BookingRepository,
// ): Promise<boolean> {
//   const conflicts = await bookingRepo.findConflicts(resourceId, timeRange);
//   return conflicts.length === 0;
// }
